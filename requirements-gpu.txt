# GPU dependency set for established optional algorithms.
# Default target: NVIDIA CUDA 12.1 PyTorch wheels for fresh environments.
# If a compatible GPU PyTorch build is already installed, do not reinstall it.
# Change the index URL to the CUDA build that matches the deployment machine.
# Installs:
# - RapidOCR text protection with ONNX Runtime GPU
# - SAM2 segmentation with CUDA-enabled PyTorch
# - ONNX fast neural style transfer with ONNX Runtime GPU
--index-url https://download.pytorch.org/whl/cu121
--extra-index-url https://pypi.org/simple

-r requirements.txt
numpy>=1.26.0
rapidocr>=3.0.0
onnxruntime-gpu>=1.18.0
torch>=2.3.0
torchvision>=0.18.0
git+https://github.com/facebookresearch/sam2.git
